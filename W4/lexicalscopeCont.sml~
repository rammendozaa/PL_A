val x = 1 (* irrelevant *)
fun f y =
    let
	val x = y + 1
    in
	fn z => x + y + z
    end

val x = 3 (* irrelevant *)
val g = f 4
val y = 5 (* irrelevant *)
val z = g 6
(* the values are irrelevant because they're being "redefined" inside the
functions that are being called *)
